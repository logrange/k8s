# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: index.docker.io/logrange/configs
  tag: latest
  pullPolicy: Always

resources: {}

podAnnotations:
  seccomp.security.alpha.kubernetes.io/pod: docker/default

nodeSelector: {}

tolerations: []

affinity: {}
